version: 1

project:
  root: .
  build_config: lzc-build.yml
  package_file: package.yml
  output: .lazycat-build/cat-led.lpk

update:
  strategy: publish
  version_source:
    type: git

build:
  toolchains:
    - kind: go
      version: 1.25.x
  run_buildscript: true

stores:
  official:
    enabled: true
    skip_if_version_exists: true
    create_if_missing: false
    changelog_locales: [zh, en]
  private:
    enabled: true
    skip_if_version_exists: true
    name: 懒猫小灯
    summary: Published from GitHub Actions
